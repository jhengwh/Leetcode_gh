## 80. Remove Duplicates from Sorted Array II

### Sol1

核心概念

- `slow`：指著「寫入位置」
- `fast`：iterator，指著「候選元素」，該元素會被考慮要不要寫到 slow 位置。
- 寫入位置 (slow) 元素的前兩個元素，必定與寫入位置元素不同。
    - 如果前者與候選元素相同，則候選元素不會被寫到 slow 位置，slow 也不會往前移動。